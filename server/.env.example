# Server Configuration
PORT=3000

# MongoDB Connection URI
MONGODB_URI=mongodb://localhost:27017/your-database-name

# JSON Web Token (JWT) Secret Key
# IMPORTANT: This MUST be a long, random, and secret string for production environments.
# A minimum of 32 characters is recommended. Use a secure random string generator.
# Example (generate one using a tool like `openssl rand -hex 32`):
JWT_SECRET=your-super-secret-and-long-jwt-key-min-32-chars-replace-me

# Web Search Tool API Key (e.g., for Google Search, Bing Search, or other providers)
# Replace with your actual API key if you intend to use the WebSearchTool with a live API
SEARCH_API_KEY=your-search-api-key

# Optional: API Key for external services (if any)
# EXTERNAL_API_KEY=your-external-api-key

# Optional: Logging level (e.g., 'info', 'debug', 'warn', 'error')
LOG_LEVEL=info

# Rate Limiting Configuration
# Window durations are set in code (e.g., 15 mins for auth, 1 hour for resource-intensive)
# Max requests per window:
AUTH_RATE_LIMIT_MAX=20
LLM_API_RATE_LIMIT_MAX=100
AGENT_RATE_LIMIT_MAX=60
FILE_UPLOAD_RATE_LIMIT_MAX=50
GENERAL_API_RATE_LIMIT_MAX=200

# Optional: If you want to make window durations configurable (in milliseconds)
# AUTH_RATE_LIMIT_WINDOW_MS=900000
# LLM_API_RATE_LIMIT_WINDOW_MS=3600000
# AGENT_RATE_LIMIT_WINDOW_MS=3600000
# FILE_UPLOAD_RATE_LIMIT_WINDOW_MS=3600000
# GENERAL_API_RATE_LIMIT_WINDOW_MS=900000

# File System Configuration
# Base directory for user uploads
UPLOAD_DIR=./uploads
# (Ensure this path is writable by the server process and properly secured)

# Max file size for uploads (in bytes)
FILE_SIZE_LIMIT_BYTES=10485760 # 10MB example

# Add other environment variables as needed by your application
# For example:
# PAYMENT_GATEWAY_API_KEY=your-payment-gateway-key
# EMAIL_SERVICE_USER=your-email-user
# EMAIL_SERVICE_PASS=your-email-password
